/* -Quimey Alejo Fontan*/
function mostrar() {
	let edadIngresada;
	let mensaje;

	edadIngresada = parseInt(document.getElementById("txtIdEdad").value);

	if (edadIngresada >= 18) {
		mensaje = "Eres mayor de edad.";
	}
}